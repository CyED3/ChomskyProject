database {
    host = "localhost";
    port = 5432;
    password = ${DB_PASSWORD};
    replica {
        host = "replica.internal";
        port = 5432;
        password = ${REPLICA_DB_PASSWORD};
    }
}

api {
    base_url = "https://api.example.com";
    token = ${API_TOKEN};
    timeout = 30;
    debug = false;
}
